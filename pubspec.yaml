name: tiktok_slim
description: Alternativa leve ao TikTok baseada em WebView.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.4.2
  webview_flutter_android: ^3.16.2

flutter:
  uses-material-design: true
